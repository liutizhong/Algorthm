package yougou.chapter1;/** * Created by liutizhong on 2015/12/10. * 快速排序 * 快速排序用到了递归程序 * 快速排序思想：先选中首个元素 */public class Quicksort {    static void sort(int arr[], int left, int right) {        if (left >= right) {            return;        }        int temp = arr[left];        int i = left;        int j = right;        //        while (i < j) {            //先从右向左寻找大于temp的数，找到后将该数放到arr[left]位置            while (j > i && arr[j] > temp) {                j--;            }            arr[i] = arr[j];            //然后从左向右寻找小于等于temp的数，如果有则将temp数归放置arr[j]处            while (i < j && arr[i] <= temp) {                i++;            }            arr[j] = arr[i];        }        arr[i] = temp;//将开始时选取的数赋值给中间位置        sort(arr, left, i - 1);        sort(arr, i + 1, right);    }    public static void main(String[] args) {        int[] m = {49, 38, 65, 97, 76, 13, 27, 49};        sort(m, 0, m.length - 1);        for (int i = 0; i < m.length; i++) {            System.out.print("\t" + m[i]);        }    }}